# Personal Profile (Express + EJS)

A small Express/EJS exercise rendering a personal profile page (name, age, hobbies, profession) from server-side variables.

## Tech stack

- Node.js, Express, EJS

## Running it

```bash
npm install
node app.js
```

Then open `http://localhost:3000`.
